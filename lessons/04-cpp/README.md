# Lesson 4: C++ and PlatformIO practice

This lesson will make a small, testable change to Teensy-targeted C++ code. The
student will build it through PlatformIO and read compiler errors when the code
is wrong.

No exercise will upload firmware automatically. Completion will require a local
build and a passing GitHub Actions build.
